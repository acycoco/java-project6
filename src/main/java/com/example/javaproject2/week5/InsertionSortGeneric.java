package com.example.javaproject2.week5;

import java.util.Arrays;
/*

public class InsertionSortGeneric<T> {
    public T[] sort(T[] arr, boolean isAscending){


        for (int i = 1; i < arr.length; i++) { //1부터
            for (int j = i; j > 0; j--) { //j는 거꾸로 내려감

                if ((isAscending? arr[j-1] - arr[j] : arr[j] - arr[j-1]) > 0){ //오름차순
                    int temp = arr[j-1];
                    arr[j-1] = arr[j];
                    arr[j] = temp;
                }
                else {
                    break;
                }
            }

        }
        return arr;
    }

    public T[] sort(T[] arr) { //메소드 오버로딩
        return sort(arr, true);
    }
    public static void main(String[] args) {
        int[] arr = {7, 2, 3, 9, 28, 11};
        char[] cArr = {'f','e','a','g','c'};
        InsertionSortGeneric<Character> is3 = new InsertionSortGeneric();
        is3.sort(cArr);
        System.out.println(Arrays.toString(arr));
        is3.sort(cArr, false);
        System.out.println(Arrays.toString(arr));


    }
}
*/
